# 管理后台数字孪生驾驶舱视觉验收

## 验收基线

- 设计规范：`docs/superpowers/specs/2026-07-14-admin-digital-twin-design-system.md`
- 设计图：`docs/design/admin-digital-twin/01-login.png` 至 `19-settings.png`
- 验收视口：1440 × 1024；补充移动断点 390 × 844
- 实现地址：`http://127.0.0.1:5241/admin/dashboard`

## 对照结果

| 页面 | 对照文件 | 结论 |
| --- | --- | --- |
| 运营总览 | `docs/verification/admin-digital-twin/dashboard-compare.png` | 通过。208px 侧栏、56px 顶栏、指标列、景区主画布、告警/服务状态和底部趋势层级一致。实现使用项目真实景区资产；指标展示真实接口数据。 |
| 数字人直播 | `docs/verification/admin-digital-twin/live-compare.png` | 通过。状态指标、脚本队列、发布操作与数字人预览形成明确双栏工作流；空数据状态不伪造已发布版本。 |
| 知识库对接站 | `docs/verification/admin-digital-twin/knowledge-compare.png` | 通过。目录、知识库工作区、工具条和同步入口保持高密度暗色工作台；当前后端为空时展示明确空状态。 |
| 登录页 | 浏览器 1440 × 1024 实测 | 通过。景区实景主视觉、运营摘要、深色登录工作区、默认账号与服务状态均单屏完整显示。 |
| 景点管理 | 浏览器 1440 × 1024 实测 | 通过。指标条、搜索、园区筛选、刷新、CSV 导出、暗色表格和分页可用。 |
| 问答记录查询 | 浏览器 1440 × 1024 实测 | 通过。会话指标、关键词/时间筛选、真实会话列表和消息详情抽屉可用。 |
| 系统设置 | 浏览器 1440 × 1024 实测 | 通过。能力标签、模型列表和当前配置保持双栏，原有保存、连接测试和模型测试能力保留。 |

## 全路由检查

浏览器逐页访问以下 18 个后台路由，全部正确显示统一顶部标题和公共页面框架，页面根节点横向溢出均为 `0px`：

`dashboard`、`home-config`、`spots`、`spots/categories`、`spots/facilities`、`routes`、`travel-tips`、`avatar`、`model-emotion`、`travel-analytics`、`scenic-structured`、`voice-scripts`、`feedback`、`live-broadcast`、`qa`、`ai-models`、`knowledge`、`setting`。

## 交互与响应式

- 问答列表 5 个详情入口均可识别，点击首条后“会话详情”抽屉正常显示并请求真实消息。
- 390 × 844 下侧栏切换为抽屉菜单，总览指标改为双列，地图与告警按纵向排列，页面可以正常滚动且没有横向页面溢出。
- 登录页在窄屏下隐藏运营预览区，保留完整登录工作区并维持单屏优先。
- 数据为空时使用业务化空状态，不用设计稿示例数据冒充接口结果。

## 已处理差异

- 设计稿地图为概念数字孪生图，实现使用仓库现有景区实景资产并叠加真实 UI 图层、热点和控制器，避免引入不可追溯素材。
- 设计稿中的统计样例仅用于视觉密度；实现全部使用后台接口返回值，因此数值、列表数量与设计稿可不同。
- 知识库、直播在当前数据库为空时保持设计结构并展示可操作空状态；创建、发布、连接配置入口仍然可用。

## 缺陷分级

- P0：0
- P1：0
- P2：0
- 非阻断项：Vite 仍提示主包体积较大，属于既有依赖集中加载问题，不影响本次页面功能与视觉验收。

final result: passed
