# 数据库地图点位补齐验证记录

## 执行结果

2026-07-20 直接向本机 `digitalhuman.scenic_facilities` 新增 24 条地图可见记录。操作使用单个 MySQL 事务完成，原有记录未修改或删除。

## 持久化验证

提交后使用新数据库连接重新查询，结果如下：

| 分类 | 可见点位数 |
| --- | ---: |
| 卫生间 | 6 |
| 景点 | 7 |
| 景区摊位 | 6 |
| 打卡点 | 6 |
| 合计 | 25 |

新增记录验证结果：

- 新增数量：24。
- 名称、分类、经度、纬度或可见性无效的记录：0。
- 超出预定灵山景区坐标范围的记录：0。
- 名称、简介或位置描述包含“模拟”“测试”“假数据”的记录：0。
- 重复非空 `spot_code`：0。

## 原有数据保护

提交后再次读取原有 ID 1、2、3，其编码、名称、分类、坐标、可见性、创建时间、更新时间和删除时间与写入前快照一致。

## 应用链路

游客端 `/api/user/scenic/facilities` 由 `AdminScenicFacilityService.getMapVisibleFacilities()` 查询 `scenic_facilities`，同时过滤点位和分类的地图可见状态。本次 24 条新增记录及其 4 个分类均符合该查询条件。

验证时临时启动本机后端，并使用默认游客账号调用受保护的 `/api/user/scenic/facilities`。接口返回 25 条记录，分类数量为卫生间 6、景点 7、景区摊位 6、打卡点 6，名称或坐标无效的记录为 0。验证完成后已正常停止临时后端进程。
